import { FormatNumWithCommasPipe } from './format-num-with-commas.pipe';

describe('FormatNumWithCommasPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatNumWithCommasPipe();
    expect(pipe).toBeTruthy();
  });
});
